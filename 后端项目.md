```XML
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">

    <!-- 私服账号 -->
    <servers>
        <server>
            <id>foshanindai-release-maven-virtual</id>
            <username>srd18027572698</username>
            <password>2098285c242093bfa13ed8cd8a3d36dd</password>
        </server>
    </servers>


    <profiles>

        <profile>
            <id>eshore</id>

            <repositories>

                <!-- 公司二方库 -->

                <repository>

                    <id>foshanindai-release-maven-virtual</id>
                    <name>foshanindai-release-maven-virtual</name>
                    <url>https://gz01-srdart.srdcloud.cn/maven/foshanindai/foshanindai-release-maven-virtual/</url>

                    <releases>
                        <enabled>true</enabled>
                    </releases>

                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>

                </repository>


                <!-- Maven中央仓库（三方库） -->
                <repository>
                    <id>central</id>
                    <name>Maven Central</name>
                    <url>
                        https://repo.maven.apache.org/maven2
                    </url>

                    <releases>
                        <enabled>true</enabled>
                    </releases>

                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>

                </repository>

            </repositories>


            <!-- 插件仓库 -->
            <pluginRepositories>

                <pluginRepository>
                    <id>central</id>
                    <name>Maven Central Plugin Repository</name>
                    <url>
                        https://repo.maven.apache.org/maven2
                    </url>

                    <releases>
                        <enabled>true</enabled>
                    </releases>

                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>

                </pluginRepository>


            </pluginRepositories>


        </profile>


    </profiles>


    <activeProfiles>
        <activeProfile>eshore</activeProfile>
    </activeProfiles>

</settings>
```

以上是maven的setting.xml文件配置，注意id要对齐，还有url，不用的项目的仓库地址不同

<br />

<br />

# IDEA 与 Docker 实战开发及踩坑排查笔记

### 知识点一：IDEA 中可视化管理本地 Docker 容器与镜像

## 问题

在 IDEA 中安装了 Docker 插件和客户端后，如何直接通过插件查看和管理本地启动了哪些容器和镜像？

## 错误理解

一开始容易认为 IDEA 作为开发工具，看到的内容可能与本地终端不一致，或者以为在 IDEA 中使用 Docker 需要复杂的网络交互和额外指令，甚至担心 IDEA 会脱离本地 Docker 环境独立运行。

## 正确认识

IDEA 的 Docker 插件实际上是一个极其强大的“可视化 Docker 客户端”。

- **绝对同步**：在 IDEA 里面看到的所有容器状态、日志、镜像列表，与在本地 cmd/PowerShell 里输入 `docker ps` 和 `docker images` 看到的结果是 **100% 实时同步** 的。
- **通信原理**：在 Windows 体系下，IDEA 是通过 Windows 本地命名管道（`npipe:////./pipe/docker_engine`）直接与后台运行的 Docker Engine（服务端）进行进程间通信的，无需连接外网。

## 排查过程

- **定位与连接思路**：我们要解决的是“如何在图形化界面中统一管理本地环境”，因此第一步必须让 IDEA 捕获到本地 Docker 后台服务。
- **具体实施步骤**：
  1. 确保电脑系统的系统托盘中 **Docker Desktop** 已正常启动运行。
  2. 按 `Ctrl + Alt + S` 打开 Settings，依次定位到 **Build, Execution, Deployment -> Docker**。
  3. 点击 `+` 号添加配置，连接方式选择 **Docker for Windows**。
  4. 为什么选择该选项？因为 Windows 下 Docker 默认通过本地命名管道暴露接口，该选项能自动识别，当下方提示 `Connection successful` 时，说明客户端与服务端的底层管道已打通。
  5. 确认并保存后，按快捷键 **`Alt + 8`** 打开底部的 **Services（服务）** 窗口。双击或点击绿色播放键启动连接，即可通过展开 `Containers`（容器日志、内置终端）和 `Images`（一键创容器、删镜像）进行极速交互。

### 知识点二：5000ms 连接超时报错与本地/远程概念的混淆纠偏

## 问题

在尝试使用插件的过程中，遇到了 `Docker Registry registry-1.docker.io ERROR: Request timeout has expired [url=[https://hub.docker.com/v2/users/login](https://hub.docker.com/v2/users/login), request_timeout=5000 ms]` 的报错，同时去寻找和配置了自己的 Docker 账号与密码。

## 错误理解

- **概念混淆（最大的坑）**：把“查看本地启动了什么容器（连接本地 Docker Engine）”**与**“登录远程镜像仓库（Docker Registry）”强行绑定在了同一个流程里。以为想用 IDEA 的 Docker 插件，就必须先登录 Docker 官方账号，从而误入了 `Docker Registry` 设置页面。
- **凭据误区**：认为本地忘记的密码可以通过某种命令或配置文件直接“查看明文”。

## 正确认识

- **核心纠偏**：**“连接本地 Docker”与“连接 Docker Registry”是完全无关的两个独立功能！**
  - **本地连接**：看本地容器、查日志、做项目开发，**完全不需要账号密码，也不需要连接外网**，它只是你的电脑（IDEA）与你的电脑（Docker 后台）在对话。
  - **Registry（镜像仓库）**：只有当你把本地做好的镜像 **Push（发布）** 到网络仓库（如 Docker Hub、阿里云），或者从私有库拉取镜像时，才需要配置远程仓库账号。
- **密码保护机制**：出于安全，Docker 本地只保存加密 Token 或由系统凭据管理器托管，无法直接查看明文密码。
- **超时根本原因**：报错 `5000 ms` 超时仅仅是因为 Docker Hub 官方服务器在国外，IDEA 发起的 HTTP 登录请求被国内网络环境拦截或超时，**这个报错对你管理本地容器没有任何影响**。

## 排查过程

- **排查思路**：用户提出了致命性疑问：“但不是直接连接本地的 docker 客户端就可以了吗？”——这正是破局的关键。我们要做的不是去死磕复杂的网络代理来让 Registry 不报错，而是**做减法，把多余的远程登录操作果断剥离**。
- **具体定位与纠偏步骤**：
  1. **密码找回思路**：确认账号可以看客户端右上角或敲 `docker info` 查看 `Username` 字段；密码无法查明文，唯一稳妥做法是去 Docker Hub 官网通过邮箱执行 `Forgot password?` 重置。
  2. **切断超时干扰**：进入 IDEA 的 `Settings -> Docker -> Docker Registry`，选中报错的 `registry-1.docker.io`，直接点击 `-` 号**删掉它**。
  3. **为什么敢直接删？** 因为我们理清了原理，删掉远程 Registry 绝对不会影响本地 Socket/命名管道的通信。
  4. **回归核心**：视线移回上一级菜单 `Settings -> Build, Execution, Deployment -> Docker`，只要这里显示绿色的 `Connection successful`，不用填任何账号密码，直接打开 Services 窗口，所有本地容器和镜像一览无余。

### 知识点三：将项目中的 SQL 初始化脚本运行到 Docker 数据库容器中

## 问题

开发项目中包含一个初始化测试数据的 `.sql` 脚本，如何让运行在 Docker 容器中的数据库（如 MySQL/PostgreSQL）执行该脚本，从而载入测试数据？

## 错误理解

可能会以为必须把项目里的 `.sql` 文件物理复制到 Docker 容器内部的 Linux 文件系统中，然后再通过 `docker exec` 进入容器终端使用命令行（如 `mysql -u... < xxx.sql`）手动去敲命令导入；或者以为必须在 Dockerfile 里配置复杂的数据卷挂载。

## 正确认识

最优雅、高效的开发流程是：**利用“端口映射（Port Bindings）”机制，借助 IDEA 自带的 Database 数据库工具窗口，把 Docker 里的数据库当作本地数据库直接连接，在图形界面的协助下一键运行脚本。**

容器虽然是隔离的，但一旦把它的服务端口映射到了宿主机（开发电脑），我们通过 `localhost` 就能透明地直接访问到容器内部的数据库服务。

## 排查过程

- **排查与落实思路**：要打通“项目代码中的 SQL 文件”与“隔离容器中的数据库”之间的壁垒，只需要以 IDEA 为桥梁：先查出容器在本地开放的端口 -> 用 IDEA 连上这个端口 -> 使用 IDEA 的驱动执行脚本。
- **具体落实步骤**：
  1. **明确映射端口（找路）**：打开 `Services`（`Alt + 8`）窗口，选中正在运行的数据库容器，在右侧面板查看 `Port Bindings`（如 `3306:3306`）。前一个数字代表宿主机（你本地电脑）监听的端口。为什么一定要看？因为有时容器映射到本地的端口可能不是默认端口（例如为了防止冲突映射成了 `3307:3306`）。
  2. **建立可视化连接（修桥）**：点击 IDEA 界面最右侧边栏的 **Database** 图标 -> `+` -> `Data Source` 选择对应数据库（如 MySQL）。
     - `Host` 填 `localhost`；`Port` 填第一步查到的本地映射端口。
     - 填入启动 Docker 容器时设置的环境变量账号与密码。
     - 若提示 `Missing driver files`，点击 `Download` 自动下载驱动后，点击 `Test Connection` 验证。
  3. **一键执行脚本（通车）**：
     - **快速做法**：在左侧 `Project` 项目文件树中，找到待执行的 `.sql` 脚本文件 -> **右键点击它** -> 选择 **`Run '你的脚本名.sql'...`**。
     - **指定靶心**：在弹出的目标窗口中，选择刚才在 Database 面板里新建并连通的 Docker 数据库连接。
     - **实时验证**：点击 Run，为什么这里最推荐这个方法？因为它不需要你关心终端语法和编排，执行完毕后直接展开右侧 Database 窗口里的表结构，双击即可像看 Excel 一样实时核验 Docker 数据库里的测试数据是否成功插入。

